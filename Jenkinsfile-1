pipeline {
    agent any
    stages {
        stage('Run Docker Image') {
            steps {
                script {
                    docker.image('node:latest').inside("-w /home/jenkins/workspace/docker") {
                        sh 'node -v'
                    }
                }
            }
        }
    }
}
